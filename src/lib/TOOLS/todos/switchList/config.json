{
    "id": "switchList",
    "name": "Switch Todo List",
    "description": "Switch to a different todo list. Use this when a user wants to view a different list.",
    "modelToolName": "switchList",
    "parameters": [
        {
            "name": "listName",
            "location": "PARAMETER_LOCATION_BODY",
            "schema": {
                "type": "string",
                "description": "The name of the todo list to switch to"
            },
            "required": true
        }
    ]
}