{
    "id": "filterTodos",
    "name": "Filter Todos",
    "description": "Filter todos by tag. Use this tool when a user wants to see todos with specific tags.",
    "modelToolName": "filterTodos",
    "parameters": [
        {
            "name": "tag",
            "location": "PARAMETER_LOCATION_BODY",
            "schema": {
                "type": "string",
                "description": "The tag to filter by, or 'all' to show all todos"
            },
            "required": true
        }
    ]
}