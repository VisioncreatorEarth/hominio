{
    "id": "createList",
    "name": "Create Todo List",
    "description": "Create a new todo list with a given name. Use this when a user wants to create a new separate list for todos.",
    "modelToolName": "createList",
    "parameters": [
        {
            "name": "listName",
            "location": "PARAMETER_LOCATION_BODY",
            "schema": {
                "type": "string",
                "description": "The name of the new todo list"
            },
            "required": true
        }
    ]
}