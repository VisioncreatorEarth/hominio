{
    "id": "removeTodo",
    "name": "Remove Todo",
    "description": "Delete a todo from the list. Use this tool when a user wants to remove a todo.",
    "modelToolName": "removeTodo",
    "parameters": [
        {
            "name": "todoText",
            "location": "PARAMETER_LOCATION_BODY",
            "schema": {
                "type": "string",
                "description": "Text to search for in todo items"
            },
            "required": true
        }
    ]
}