{
    "id": "updateTodo",
    "name": "Update Todo",
    "description": "Update the text of an existing todo. Use this when a user wants to change the content of a todo item.",
    "modelToolName": "updateTodo",
    "parameters": [
        {
            "name": "oldText",
            "location": "PARAMETER_LOCATION_BODY",
            "schema": {
                "type": "string",
                "description": "The current text of the todo to update"
            },
            "required": true
        },
        {
            "name": "newText",
            "location": "PARAMETER_LOCATION_BODY",
            "schema": {
                "type": "string",
                "description": "The new text to replace the old text with"
            },
            "required": true
        }
    ]
}